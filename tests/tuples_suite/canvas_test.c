#include <raytracer/canvas.h>

#include "../bdd-for-c.h"

spec("Canvas Tests")
{
    // it("Creating a canvas")
    // {
    //     canvas c = new_canvas(10, 20);

    //     /* width corresponds to num of cols */
    //     /* height corresponds to num of rows. */
    //     check(c.height == 10);
    //     check(c.width == 20);

    //     int all_black = 1;
        
    //     for (int i = 0; i < 10; ++i)
    //     {
    //         for (int j = 0; j < 20; ++j)
    //             all_black = all_black && is_black(&c, i, j);
    //     }

    //     check(all_black);

    //     c_free(&c);
    // }

    // it("Writing pixels to a canvas")
    // {
    //     canvas c = new_canvas(10, 20);

        
    // }
}